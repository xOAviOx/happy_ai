<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Platform theme only: Compose paints everything, so no Material AAR needed. -->
    <style name="Theme.Happy" parent="android:Theme.DeviceDefault.DayNight">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
    </style>
</resources>
