<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ===== Phase 0: service survival ===== -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- ===== Phase 1-2: wake word + speech ===== -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- ===== Phase 4: device control ===== -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <!-- ===== Phase 4: device control and launching apps ===== -->
    <!-- Starting an activity from a background service needs this on Android 10+.
         A foreground service alone does not earn the right. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Silent mode counts as a Do Not Disturb change. -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <!-- Required to move stream volumes at all. Normal protection level, so it is
         granted at install, but without it the calls quietly do nothing. -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- The build spec says AlarmClock intents need no permission. That is wrong:
         the clock app guards HandleApiCalls with SET_ALARM, and without this every
         alarm and timer fails with a SecurityException. Verified on vivo, whose
         clock is com.android.BBKClock. Normal protection level, granted at install. -->
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />

    <!-- ===== Phase 5: calls and messaging ===== -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ANSWER_PHONE_CALLS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <!-- ===== Phase 9: calendar, vision, location ===== -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Android 11 package visibility. Without this the launcher list comes back
         empty and every "open X" looks like an app that does not exist. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:name=".HappyApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Happy">

        <activity
            android:name=".ui.SetupActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.LogActivity"
            android:exported="false"
            android:label="@string/log_title"
            android:parentActivityName=".ui.SetupActivity" />

        <!--
            foregroundServiceType declares both specialUse and microphone.

            Android 15 forbids starting a `microphone` foreground service from a
            BOOT_COMPLETED receiver, so Happy always starts as `specialUse` and
            promotes itself to `microphone` only once it actually opens the mic
            (Phase 1). Both types must be declared here for that promotion to be
            legal at runtime.
        -->
        <service
            android:name=".service.HappyService"
            android:exported="false"
            android:foregroundServiceType="specialUse|microphone"
            android:stopWithTask="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Always-on wake word listening for a personal, sideloaded voice assistant." />
        </service>

        <!--
            Bound by the system when the user grants notification access. This is
            what makes "read my messages" and inline reply work without any
            accessibility scripting.
        -->
        <service
            android:name=".service.HappyNotificationListener"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <receiver
            android:name=".service.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
